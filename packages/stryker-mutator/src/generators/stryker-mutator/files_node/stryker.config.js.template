module.exports = {
  testRunner: 'jest',
  packageManager: '<%= packageManager %>',
  inPlace: false,
  reporters: ['html', 'clear-text', 'progress'],
  coverageAnalysis: 'perTest',
  tsconfigFile: './<%= projectRoot %>/tsconfig.spec.json',
  mutate: ['./<%= sourceRoot %>/**/*.ts',
    '!./<%= sourceRoot %>/**/*.spec.ts',
    '!./<%= sourceRoot %>/main.ts',
  ],
  ignorePatterns: ['dist', 'coverage', 'node_modules'],
  plugins: ['@stryker-mutator/jest-runner'],
  disableTypeChecks: './**/*.{js,ts,jsx,tsx,html,vue}',
  jest: {
    projectType: 'custom',
    configFile: './<%= projectRoot %>/jest.config.js',
    enableFindRelatedTests: true,
  },
  commandRunner: {command: 'nx test <%= projectName %>'},
  buildCommand: 'nx build <%= projectName %>',
  ignoreStatic: true,
  htmlReporter: {
    baseDir: 'dist/reports/mutation/html/<%= projectRoot %>'
  }
};
